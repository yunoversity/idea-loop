---
description: Ingest a free-form painpoint dump (and process any pending inbox/ files)
---

Use the intake agent to process the following dump into a structured painpoint file
(see its playbook in .claude/agents/intake.md). Also process any files sitting in
inbox/ first. Capture never bounces — extract what's there, queue the rest as open
questions.

Dump:
$ARGUMENTS
