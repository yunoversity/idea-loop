---
description: Park a painpoint with reason + revival criteria (never delete)
---

Anthony wants to park the painpoint below. As chief-of-staff:

1. Draft the parking record: reason, and the specific evidence that would revive it.
   Confirm both with Anthony before writing anything.
2. On confirmation: set status `parked`, fill `parked_reason` and `revival_criteria`
   frontmatter, log it, and append the decision + reason to taste-profile.md.

Nothing is ever deleted. Parked items resurface at the monthly parked review.

Painpoint: $ARGUMENTS
